[project]
name = "GreenTrace"
version = "0.1.0"
description = "Effortless CO₂ Emissions Tracking for Python Code"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "psutil>=7.2.2",
    "py-cpuinfo>=9.0.0",
    "pytest>=9.0.2",
    "pytest-asyncio>=1.3.0",
]
